main: def(args: [str]) -> int {
  for arg in args {
    match arg {
      | "fib" => fib(100)
      | "fac" => fac(100)
    }
  }
}



fib: def(a: int) -> int {
  if a < 3 {
    return a
  }

  fib(a - 1) + fib(a - 2)
}

fac: def(a: int) -> int {
  if a < 3 {
    return a
  }

  fac(a - 1) * a
}